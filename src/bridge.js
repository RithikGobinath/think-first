// Runs in the isolated world on chess.com. Owns chrome.storage access and
// relays settings to inject.js (MAIN world) over window.postMessage.
(function () {
  "use strict";

  console.log("[Think First] bridge.js loaded");
})();
